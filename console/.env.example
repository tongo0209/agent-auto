# Credential cho nút "Đánh Done" trên console (Jira REST v3, Basic auth).
# Chép file này thành `console/.env` rồi điền — .env đã nằm trong .gitignore, KHÔNG commit.
#
# Tạo token: https://id.atlassian.com/manage-profile/security/api-tokens
# Token chỉ hiện MỘT LẦN lúc tạo, copy ngay.

JIRA_EMAIL=email-vng-cua-ban@vng.com.vn
JIRA_TOKEN=dán-api-token-vào-đây
